package image

import "testing"

func TestConvert(t *testing.T) {
	type testCase struct {
		name          string
		inPath        string
		outPath       string
		expectedError error
		expectedOut   bool
	}

}
